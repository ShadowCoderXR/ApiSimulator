spring:
  application:
    name: api-simulator
  datasource:
    url: jdbc:mysql://localhost:3306/apisimulator?allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: Lolitos76170604!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

server:
  port: 8001